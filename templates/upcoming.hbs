<div style="gap:10px;align-items:flex-start; margin-bottom: 12px;">
  <div>
    <div style="font-size:14px;font-weight:600;color:var(--navy)"><a href="{{result.url}}" target="_blank">{{result.title}}</a></div>
    <p style="font-size:12px;color:#555;margin:3px 0 0;line-height:1.5">{{result.description}}</p>
  </div>
</div>